"""Delivery calculation.

The eventual chain is per $100 allocated to Indonesia, not Indonesia's share
of a global $100. Collection capacity is deliberately absent from the model.
"""


SCENARIOS = ("cautious", "central", "fast")


def main() -> None:
    raise SystemExit(
        "Delivery inputs are pending primary-source verification; "
        "see docs/parameters_and_sources.md."
    )


if __name__ == "__main__":
    main()
